Общая информация
===============

Это директория для скриптов миграции базы данных, используемых Alembic.

Использование
---------

Для создания новой миграции автоматически:

```
alembic revision --autogenerate -m "Описание изменений"
```

Для применения миграций:

```
alembic upgrade head
```

Для отката последней миграции:

```
alembic downgrade -1
```

Для получения информации о текущей версии:

```
alembic current
```

Для получения истории миграций:

```
alembic history --verbose
```

Подробнее о командах и опциях Alembic можно узнать в документации:
https://alembic.sqlalchemy.org/en/latest/ 